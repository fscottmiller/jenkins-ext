@Library('jenkins-ext') _

require 'helm'
require 'kubectl'

stage('will this work') {
    echo "idk"
}

pipeline {
    agent any

    stages {
        stage('test') {
            steps {

            }
        }
    }
}