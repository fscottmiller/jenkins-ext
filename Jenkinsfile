@Library('jenkins-ext') _

require 'nodejs'
require 'ruby'
require 'nodejs'
initialize this

kubepipe(serviceAccount: 'jenkins-admin') {
    python '--version'
    ruby '--version'
    nodejs '--version'
}
