@Library('jenkins-ext') _

require 'helm'
require 'kubectl'

stage('will this work') {
    steps {
        echo "idk"
    }
}

pipeline {
    agent any

    stages {
        stage('test') {
            steps {

            }
        }
    }
}